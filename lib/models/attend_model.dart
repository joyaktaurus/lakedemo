import 'dart:ui';

class CalendarEvent {
  final String? eventName;
  final String? eventDate;
  final String? eventBackgroundColor;

  CalendarEvent({
    required this.eventName,
    required this.eventDate,
    required this.eventBackgroundColor,
  });
}

